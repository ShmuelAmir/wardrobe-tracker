import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function StatsTab() {
  return (
    <PlaceholderScreen
      title="Stats"
      description="Most worn, least worn and never worn — all derived from wear events."
      testID="stats-screen"
    />
  );
}
